# CLAUDE.md

## プロジェクト概要

faby は日本株・グローバル株式市場のデータ可視化・分析ダッシュボード。
yfinance からリアルタイムデータを取得し、Streamlit で対話的に表示する。
[claude_project](https://github.com/kfuru1984/claude_project) のマルチエージェント分析システムと連携することを想定している。

## 投資分析のルール

### 情報の分類
- **事実 (Fact)**: 検証可能なデータ。必ず出典を明記する。
- **ガイダンス (Guidance)**: 経営陣が発表した見通し。
- **推測 (Speculation)**: 分析者の解釈。必ず「推測」と明記する。

### 禁止事項
- 売買の推奨（「買いです」「売るべきです」は禁止）
- 未確認の数値を事実として記載
- 楽観的すぎる結論（リスク要因を必ず3つ以上挙げること）

### 特記事項・用語ルール
- NVIDIAのCEOは「Jensen Huang」と正確に表記すること
- 米国JOLTS（求人労働移動調査）に関する言及では、正確なコンポーネント用語を使用すること

## 開発ガイドライン

### コード規約
- Python 3.11+ を使用
- 型ヒントを積極的に活用する
- 関数・モジュールは単一責任の原則に従う
- Streamlit のキャッシュ（`@st.cache_data`）を適切に使用してパフォーマンスを確保する

### データ取得
- yfinance を使用。レート制限に注意し、TTL キャッシュ（デフォルト5分）を必ず設定する
- データ取得失敗時は例外を握りつぶさず、ユーザーに明示的にエラーを表示する

### UIコンポーネント
- 日本語ラベルを使用する
- 価格は通貨（JPY/USD）に応じた適切なフォーマットで表示する
- モバイル対応を意識したレイアウト設計を心がける

### テスト
- データ取得ロジック（`data/fetcher.py`）には単体テストを作成する
- UIコンポーネントの動作確認は `streamlit run app.py` で手動確認する
